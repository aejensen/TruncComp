% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CI.R
\name{confint.trunc_comp_fit}
\alias{confint.trunc_comp_fit}
\title{Confidence intervals for a truncated-comparison fit}
\usage{
\method{confint}{trunc_comp_fit}(
  object,
  parameter = c("mu_delta", "alpha_delta"),
  method = "welch",
  mu_delta = NULL,
  log_or_delta = NULL,
  conf.level = object$conf.level,
  plot = TRUE,
  offset = NULL,
  resolution = 35,
  ...
)
}
\arguments{
\item{object}{A \code{"trunc_comp_fit"} object returned by \code{\link[=trunc_comp]{trunc_comp()}}.}

\item{parameter}{Parameter selection for the requested interval. Use
\code{"mu_delta"} and/or \code{"alpha_delta"} for the stored component intervals,
\code{"delta"} for the derived coded endpoint mean difference, or \code{"joint"} for
the two-parameter likelihood ratio surface.}

\item{method}{Interval construction for \code{parameter = "delta"}. One of
\code{"welch"}, \code{"profile"}, or \code{"projected"}.}

\item{mu_delta}{Optional grid values for the mean-difference axis when
\code{parameter = "joint"}.}

\item{log_or_delta}{Optional grid values for the log odds ratio axis when
\code{parameter = "joint"}.}

\item{conf.level}{Confidence level for the interval or contour threshold.}

\item{plot}{Logical; if \code{TRUE}, plot the joint confidence surface.}

\item{offset}{Optional simultaneous-grid expansion. If omitted, a
data-adaptive default is derived from the fitted marginal intervals or from
fallback data scales. A single number is applied to both axes; a length-2
vector supplies separate expansions for \code{mu_delta} and \code{log_or_delta}.}

\item{resolution}{Number of grid points per axis for joint surface
calculations.}

\item{...}{Additional arguments are not supported.}
}
\value{
Invisibly returns a printed matrix for the component and \code{delta}
intervals, or a list with the evaluated joint surface for
\code{parameter = "joint"}.
}
\description{
Computes component confidence intervals, joint confidence region surfaces,
and on-demand \code{delta} intervals for a fitted \code{"trunc_comp_fit"} object.
}
\details{
Joint component regions are available for successful adjusted and
unadjusted \code{lrt} and \code{splrt} fits. For adjusted fits, the two displayed axes
are the treatment coefficients in the conditional non-atom mean and logit
models, with all adjustment coefficients profiled out. The \code{delta} intervals
are available only for unadjusted fits because adjusted conditional
coefficients do not by themselves define a marginal coded-scale contrast.
The projected \code{delta} interval maps a full four-parameter likelihood region
using a chi-square cutoff with four degrees of freedom. The profile interval
imposes the scalar \code{delta} constraint on that full criterion and uses one
degree of freedom.
}
\examples{
library(TruncComp)
data("trunc_comp_example", package = "TruncComp")
fit <- trunc_comp(Y ~ R, atom = 0, data = trunc_comp_example, method = "lrt")
confint(fit)
confint(fit, parameter = "joint", plot = FALSE, resolution = 10)
confint(fit, parameter = "delta", method = "profile")
}
