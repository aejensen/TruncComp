% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesPPC.R
\name{posterior_predictive_pvalues}
\alias{posterior_predictive_pvalues}
\title{Posterior predictive p-values for a Bayesian truncated-comparison fit}
\usage{
posterior_predictive_pvalues(object, ndraws = NULL, seed = NULL)
}
\arguments{
\item{object}{A successful \code{"trunc_comp_bayes_fit"} object returned by
\code{\link[=trunc_comp_bayes]{trunc_comp_bayes()}}.}

\item{ndraws}{Optional number of posterior draws to use when simulating
predictive replications. If \code{NULL}, uses the cached default summary based
on \code{min(200, n_posterior_draws)} draws.}

\item{seed}{Optional non-negative integer used to make the predictive
p-values reproducible. If \code{NULL}, defaults to the Stan seed stored in the
fit when available.}
}
\value{
A data frame with one row for the atom PPC and one row for the
continuous PPC. The returned columns are \code{p_value}, \code{statistic}, \code{scale},
\code{ndraws}, \code{mean_observed_discrepancy}, and
\code{mean_replicated_discrepancy}.
}
\description{
Computes discrepancy-based posterior predictive p-values for the atom and
continuous parts of a successful Bayesian two-part fit. The atom p-value is
based on the maximum armwise absolute deviation between the observed atom
rate and the fitted atom probability. The continuous p-value is based on the
maximum armwise Kolmogorov-Smirnov discrepancy between the empirical CDF of
the observed non-atom outcomes and the fitted continuous mixture CDF. These
are posterior predictive model-checking summaries, not frequentist
hypothesis-test p-values.
}
\examples{
\dontrun{
data("trunc_comp_example", package = "TruncComp2")
fit <- trunc_comp_bayes(
  Y ~ R,
  atom = 0,
  data = trunc_comp_example,
  chains = 4,
  iter_warmup = 500,
  iter_sampling = 1000,
  refresh = 0
)

posterior_predictive_pvalues(fit)
}
}
