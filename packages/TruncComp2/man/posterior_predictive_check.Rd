% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesPPC.R
\name{posterior_predictive_check}
\alias{posterior_predictive_check}
\title{Posterior predictive checks for a Bayesian truncated-comparison fit}
\usage{
posterior_predictive_check(
  object,
  type = c("both", "atom", "continuous"),
  ndraws = 50,
  seed = NULL
)
}
\arguments{
\item{object}{A successful \code{"trunc_comp_bayes_fit"} object returned by
\code{\link[=trunc_comp_bayes]{trunc_comp_bayes()}}.}

\item{type}{Which posterior predictive checks to return: both plots, only the
atom PPC, or only the continuous PPC.}

\item{ndraws}{Number of posterior draws to use when simulating predictive
replications. If \code{ndraws} exceeds the available number of posterior draws,
it is clipped to the available size.}

\item{seed}{Optional non-negative integer used to make the predictive checks
reproducible.}
}
\value{
If \code{type = "both"}, a named list with \code{atom} and \code{continuous}
\code{ggplot2} objects. Otherwise returns the requested \code{ggplot2} object. Each
plot subtitle reports the corresponding posterior predictive p-value.
}
\description{
Uses \href{https://mc-stan.org/bayesplot/}{bayesplot} to visualize posterior
predictive checks for the experimental Bayesian two-part model. The atom PPC
checks the Bernoulli atom model across all subjects, while the continuous PPC
checks the conditional non-atom mixture model among the observed outcomes.
For \code{continuous_support = "real_line"} this uses Gaussian predictive draws,
while \code{continuous_support = "positive_real"} uses Gamma predictive draws on
the original positive outcome scale and displays the continuous PPC as a
density overlay on \code{log(Y)} to avoid boundary artifacts at zero.
}
\examples{
\dontrun{
data("trunc_comp_example", package = "TruncComp2")
fit <- trunc_comp_bayes(
  Y ~ R,
  atom = 0,
  data = trunc_comp_example,
  chains = 4,
  iter_warmup = 500,
  iter_sampling = 1000,
  refresh = 0
)

ppc <- posterior_predictive_check(fit, seed = 1)
ppc$atom
ppc$continuous
}
}
