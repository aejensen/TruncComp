% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truncComp.R
\name{trunc_comp}
\alias{trunc_comp}
\alias{trunc_comp.formula}
\alias{trunc_comp.default}
\alias{truncComp}
\title{Fit the TruncComp2 two-sample comparison model}
\usage{
trunc_comp(formula, ...)

\method{trunc_comp}{formula}(
  formula,
  atom,
  data,
  method = c("lrt", "splrt"),
  conf.level = 0.95,
  init = NULL,
  adjust = NULL,
  ...
)

\method{trunc_comp}{default}(
  formula,
  a,
  r,
  method = c("lrt", "splrt"),
  conf.level = 0.95,
  init = NULL,
  adjust = NULL,
  atom = NULL,
  ...
)

truncComp(formula, ...)
}
\arguments{
\item{formula}{For the formula interface, a formula with a continuous outcome
on the left-hand side and a single binary treatment indicator on the
right-hand side. For the default interface, the first positional argument
is the outcome vector.}

\item{...}{Unused additional arguments.}

\item{atom}{A single numeric value used for the special atom outcome. The
observation indicator is reconstructed internally as \code{Y != atom}. For the
default interface, \code{atom} may be omitted only when \code{y[a == 0]} has exactly
one unique finite value, in which case it is inferred and stored on the
fitted object.}

\item{data}{A data frame containing the variables referenced by \code{formula}.}

\item{method}{Either \code{"lrt"} for the parametric likelihood-ratio method or
\code{"splrt"} for the semi-parametric likelihood-ratio method. Upper-case
values are accepted for compatibility.}

\item{conf.level}{Confidence level used for the reported intervals.}

\item{init}{Optional compatibility argument retained from older parametric
implementations. The current model-backed \code{LRT} path stores it on the
returned object but does not use it for estimation.}

\item{adjust}{Optional covariate adjustment. For the formula interface this
must be \code{NULL} or a one-sided additive formula such as \code{~ age + sex}. For
the default interface this must be \code{NULL}, a data frame, or a matrix of
baseline covariates. The same additive adjustment is used in both the
observed-outcome and observation components. Adjusted fits support only
component confidence intervals.}

\item{a}{Binary indicator for whether the continuous outcome is observed.}

\item{r}{Binary treatment indicator for the default interface.}
}
\value{
An S3 object of class \code{"trunc_comp_fit"} with component estimates,
confidence intervals, the joint likelihood-ratio statistic, metadata about
the fitting method, the standardized analysis data, the matched call, and
the fitted atom value. Failed fits return the same class with
\code{success = FALSE} and an error message.
}
\description{
Compares two groups when a continuous outcome has a distinguished atom value
representing an unobserved or undefined outcome.
}
\details{
For successful unadjusted fits, the package also computes a derived
combined-outcome contrast

\verb{delta = [p1 * mu1 + (1 - p1) * atom] - [p0 * mu0 + (1 - p0) * atom]}.

The fitted object stores only the \code{delta} point estimate. Confidence
intervals for \code{delta} are computed on demand through
\code{confint(fit, parameter = "delta", method = "welch" | "profile" | "projected")}.

Adjusted fits deliberately return \code{NA} for \code{delta} because the implemented
adjusted treatment effects are conditional regression coefficients rather
than standardized marginal contrasts.

\code{trunc_comp()} is the primary interface. The older camelCase entry point
\code{\link[=truncComp]{truncComp()}} remains available as a deprecated compatibility wrapper.
}
\examples{
library(TruncComp2)
f0 <- function(n) stats::rnorm(n, 3, 1)
f1 <- function(n) stats::rnorm(n, 3.5, 1)
d <- simulate_truncated_data(n = 100, f0 = f0, f1 = f1, pi0 = 0.6, pi1 = 0.5)

# Formula interface
trunc_comp(Y ~ R, atom = 0, data = d, method = "lrt")
trunc_comp(Y ~ R, atom = 0, data = d, method = "splrt")

data("trunc_comp_adjusted_example", package = "TruncComp2")
trunc_comp(Y ~ R, atom = 0, data = trunc_comp_adjusted_example, method = "lrt", adjust = ~ L)
trunc_comp(Y ~ R, atom = 0, data = trunc_comp_adjusted_example, method = "splrt", adjust = ~ L)

# Default interface
trunc_comp(d$Y, d$A, d$R, method = "lrt", atom = 0)
}
\seealso{
\code{\link[=summary.trunc_comp_fit]{summary.trunc_comp_fit()}}, \code{\link[=print.trunc_comp_fit]{print.trunc_comp_fit()}},
\code{\link[=confint.trunc_comp_fit]{confint.trunc_comp_fit()}}, \code{\link[=joint_contrast_surface]{joint_contrast_surface()}}
}
