% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CI.R
\name{joint_contrast_surface}
\alias{joint_contrast_surface}
\title{Joint confidence-region surface for an unadjusted fit}
\usage{
joint_contrast_surface(
  m,
  mu_delta = NULL,
  log_or_delta = NULL,
  conf.level = m$conf.level,
  plot = TRUE,
  offset = NULL,
  resolution = 35
)
}
\arguments{
\item{m}{A successful unadjusted \code{"trunc_comp_fit"} object fitted with
\code{method = "lrt"} or \code{method = "splrt"}.}

\item{mu_delta}{Optional grid values for the mean-difference axis.}

\item{log_or_delta}{Optional grid values for the log-odds-ratio axis.}

\item{conf.level}{Confidence level used for the plotted contour. Defaults to
\code{m$conf.level}.}

\item{plot}{Logical; if \code{TRUE}, plot the surface and contour.}

\item{offset}{Optional grid expansion. If omitted, a data-adaptive default is
derived from the fitted marginal intervals or fallback data scales. A
single number is applied to both axes; a length-2 vector supplies separate
expansions for \code{mu_delta} and \code{log_or_delta}.}

\item{resolution}{Number of grid points per axis.}
}
\value{
A list with components \code{mu_delta}, \code{log_or_delta}, and \code{surface},
containing the evaluated grid and the corresponding joint likelihood-ratio
statistics.
}
\description{
Evaluates the simultaneous confidence-region surface for a fitted
\code{"trunc_comp_fit"} object from the unadjusted parametric or semi-parametric
likelihood-ratio method.
}
\details{
This helper is exported for convenience. Internally it is a
thin wrapper around \code{\link[=jointContrastSurfaceData]{jointContrastSurfaceData()}} with \code{include_delta = FALSE}.
}
\examples{
library(TruncComp2)
data("trunc_comp_example", package = "TruncComp2")
fit <- trunc_comp(Y ~ R, atom = 0, data = trunc_comp_example, method = "splrt")
surface <- joint_contrast_surface(fit, plot = FALSE, resolution = 10)
str(surface)
}
