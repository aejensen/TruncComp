% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-interface-docs.R
\name{trunccomp-simulation-helpers}
\alias{trunccomp-simulation-helpers}
\alias{.validateSimulationInputs}
\alias{.drawObservedOutcome}
\alias{.simulateTruncatedGroup}
\title{Internal simulation helpers}
\usage{
.validateSimulationInputs(n, f0, f1, pi0, pi1, atom = 0)
.drawObservedOutcome(generator, n, label)
.simulateTruncatedGroup(n, r, generator, probability, label, atom = 0)
}
\description{
These helpers validate and implement the public simulation interface.
}
\details{
Internal simulation helpers

Developer-facing documentation for the utilities that build reproducible
simulated datasets for tests, examples, and validation scripts.
\subsection{\code{.validateSimulationInputs(n, f0, f1, pi0, pi1, atom = 0)}}{

Checks the user-supplied inputs for \code{\link[=simulate_truncated_data]{simulate_truncated_data()}}. \code{n} must be a
positive integer, \code{f0} and \code{f1} must be functions, \code{pi0} and \code{pi1} must lie
in \verb{[0, 1]}, and \code{atom} must be scalar and finite. The helper returns \code{NULL}
invisibly and throws descriptive errors on invalid inputs. Its role is to
keep the public simulator from repeating input-validation logic.
}

\subsection{\code{.drawObservedOutcome(generator, n, label)}}{

Calls a group-specific outcome generator and standardizes its output.
\code{generator} is the user-provided function, \code{n} is the requested sample size,
and \code{label} names the generator in error messages. The helper returns a
numeric vector of length \code{n}. If the generator returns a single scalar, that
first scalar is kept and the helper obtains the remaining draws by repeated
calls to \code{generator(1)}. It errors when the result is not finite numeric data
of the correct length. Its role is to make the simulation interface flexible
while keeping downstream code simple.
}

\subsection{\code{.simulateTruncatedGroup(n, r, generator, probability, label, atom = 0)}}{

Simulates one treatment arm. \code{n} is the group size, \code{r} is the treatment-arm
label to store in the \code{R} column, \code{generator} produces non-atom outcomes,
\code{probability} is the probability of being outside the atom, \code{label} is passed
to \code{.drawObservedOutcome()}, and \code{atom} represents atom observations. The
helper errors if a non-atom draw equals \code{atom}, because that value uniquely
identifies the atom in the coded endpoint. It returns a standardized data
frame with \code{R}, \code{A}, and \code{Y}. It inherits input validation failures from the
upstream helpers. Its role is to keep the public simulator symmetric across
the two treatment groups.
}
}
\seealso{
\code{\link[=simulate_truncated_data]{simulate_truncated_data()}}
}
\keyword{internal}
