% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-interface-docs.R
\name{trunccomp2-input-helpers}
\alias{trunccomp2-input-helpers}
\alias{normalizeFormulaAdjust}
\alias{prepareFormulaAdjustment}
\alias{prepareDefaultAdjustment}
\alias{resolveDefaultAtom}
\alias{truncComp_core}
\title{Internal interface normalization helpers}
\usage{
normalizeFormulaAdjust(adjust, outcome_name, treatment_name)
prepareFormulaAdjustment(data, adjust)
prepareDefaultAdjustment(adjust, n)
resolveDefaultAtom(y, a, atom = NULL)
truncComp_core(
  y, a, r, method, conf.level = 0.95, init = NULL,
  adjust_data = NULL, adjust_formula = NULL, atom = NULL
)
}
\description{
These helpers validate the formula and default interfaces, normalize optional
covariate-adjustment inputs, infer the atom value when possible, and dispatch
into the shared fitting core.
}
\details{
Internal interface normalization helpers

Developer-facing documentation for the helpers that turn the public
\code{truncComp()} inputs into the standardized \code{Y}/\code{A}/\code{R} analysis frame used by
both estimation engines.
\subsection{\code{normalizeFormulaAdjust(adjust, outcome_name, treatment_name)}}{

Validates a one-sided adjustment formula supplied through the public formula
interface. \code{adjust} must be \code{NULL} or a one-sided additive formula;
\code{outcome_name} and \code{treatment_name} are the variable names from the main
model formula and are used to reject collisions. The helper returns the
validated formula unchanged or \code{NULL} when no adjustment terms remain. It
errors on non-formulas, formulas using \code{.}, interaction terms, references to
the outcome or treatment variables, or the reserved standardized names \code{Y},
\code{A}, and \code{R}. Its role is to ensure the later model-frame construction is
unambiguous.
}

\subsection{\code{prepareFormulaAdjustment(data, adjust)}}{

Builds the adjustment-only model frame for the formula interface. \code{data} is
the user-supplied data frame and \code{adjust} is the validated one-sided formula.
The helper returns a list with \code{data} containing the covariate frame and
\code{formula} containing the original adjustment formula, or \code{NULL} components
when no adjustment variables are present. It fails if \code{na.fail} detects
missing values or if duplicate covariate names would make the later
standardized analysis data ambiguous. Its role is to keep adjustment handling
identical across \code{LRT} and \code{SPLRT}.
}

\subsection{\code{prepareDefaultAdjustment(adjust, n)}}{

Normalizes adjustment covariates passed through \code{truncComp.default()}.
\code{adjust} may be \code{NULL}, a data frame, or a matrix, and \code{n} is the expected
number of rows matching \code{y}. The helper returns a list with standardized
adjustment data and an additive formula created with \code{reformulate()}. It
errors when row counts do not match, names are missing or duplicated, the
input is neither matrix nor data frame, or reserved names would collide with
the internal \code{Y}/\code{A}/\code{R} columns. Its role is to align the default interface
with the formula interface before both enter the same core fitter.
}

\subsection{\code{resolveDefaultAtom(y, a, atom = NULL)}}{

Determines which atom value should be stored on the fitted object for the
default interface. \code{y} is the combined outcome vector, \code{a} is the observation
indicator, and \code{atom} is an optional explicit atom supplied by the caller.
The helper returns a single numeric atom. It errors when an explicit atom is
not scalar and finite, or when implicit inference is impossible because
\code{y[a == 0]} does not collapse to a single unique finite value. Its role is to
make later \code{Delta} calculations reproducible even when the user omits \code{atom}.
}

\subsection{`truncComp_core(y, a, r, method, conf.level = 0.95, init = NULL,}{

adjust_data = NULL, adjust_formula = NULL, atom = NULL)`

Shared engine used by both public interfaces after argument normalization.
\code{y}, \code{a}, and \code{r} are the standardized outcome, observation, and treatment
vectors; \code{method} chooses \code{LRT} or \code{SPLRT}; \code{conf.level}, \code{init}, and \code{atom}
are stored on the result; and \code{adjust_data} plus \code{adjust_formula} represent
optional baseline covariates. The helper returns a \code{"TruncComp2"} object from
\code{\link[=LRT]{LRT()}} or \code{\link[=SPLRT]{SPLRT()}}, augmented with the standardized analysis data and atom.
If \code{isDataOkay()} fails it returns a structured error object instead of
attempting estimation. Its role is to keep the two public interfaces thin and
to guarantee that both methods see the same canonical analysis frame.
}
}
\seealso{
\code{\link[=truncComp]{truncComp()}}, \code{\link[=truncComp.default]{truncComp.default()}}, \code{\link[=LRT]{LRT()}}, \code{\link[=SPLRT]{SPLRT()}}
}
\keyword{internal}
