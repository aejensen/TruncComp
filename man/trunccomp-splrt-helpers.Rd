% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-semiparametric-docs.R
\name{trunccomp-splrt-helpers}
\alias{trunccomp-splrt-helpers}
\alias{adjusted_SPLRT}
\alias{SPLRT}
\title{Internal semiparametric likelihood ratio helpers}
\usage{
adjusted_SPLRT(
  data, conf.level = 0.95, adjust = NULL, adjust_spec = NULL, atom = NULL,
  call = NULL
)
SPLRT(
  data, conf.level = 0.95, adjust = NULL, adjust_spec = NULL, atom = NULL,
  call = NULL
)
}
\description{
These helpers combine empirical likelihood inference for the conditional
non-atom mean contrast with logistic likelihood ratio inference for the
probability of being outside the atom.
}
\details{
Internal semiparametric likelihood ratio helpers

Developer-facing documentation for the high-level semiparametric fitting
wrappers used by \code{method = "splrt"}.
\subsection{`adjusted_SPLRT(data, conf.level = 0.95, adjust = NULL,}{

adjust_spec = NULL, atom = NULL, call = NULL)`

Fits the adjusted semiparametric procedure. \code{data} is the standardized
analysis frame, \code{conf.level} controls the marginal intervals, \code{adjust} is the
additive baseline-covariate formula, \code{adjust_spec} is the printable metadata
string stored on the result, \code{atom} is carried through for consistency, and
\code{call} stores the matched public call when available. The helper returns a
successful or failed \code{"trunc_comp_fit"} object. It rejects
irregular adjusted logistic fits using the parametric regularity checks and
delegates the non-atom component to \code{el_regression_fit()}. Its role
is to keep the adjusted \code{SPLRT} logic separate from the simpler unadjusted
empirical likelihood path while preserving the same result structure.
}

\subsection{`SPLRT(data, conf.level = 0.95, adjust = NULL, adjust_spec = NULL,}{

atom = NULL, call = NULL)`

Main semiparametric estimator called by \code{trunc_comp_core()}. In the
unadjusted case it extracts the non-atom outcomes by arm, runs the
two-sample empirical likelihood test with \code{el_mean_diff_fit()}, fits the
logistic non-atom probability models with \code{glm()}, and adds the two component
likelihood ratio statistics. When \code{adjust} is non-\code{NULL}, it delegates to
\code{adjusted_SPLRT()}. The helper returns a \code{"trunc_comp_fit"} object and, on
success, passes it through \code{augmentDeltaInference()} so unadjusted fits gain
the derived \code{delta} point estimate. Its role is to expose a single
semiparametric implementation point to the rest of the package.
}
}
\seealso{
\code{\link[=el_mean_diff_fit]{el_mean_diff_fit()}}, \code{\link[=el_regression_fit]{el_regression_fit()}}, \code{\link[=augmentDeltaInference]{augmentDeltaInference()}}
}
\keyword{internal}
