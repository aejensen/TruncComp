% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truncCompBayes.R
\name{trunc_comp_bayes}
\alias{trunc_comp_bayes}
\alias{trunc_comp_bayes.formula}
\alias{trunc_comp_bayes.default}
\title{Fit the experimental Bayesian two-sample TruncComp2 model}
\usage{
trunc_comp_bayes(formula, ...)

\method{trunc_comp_bayes}{formula}(
  formula,
  atom,
  data,
  conf.level = 0.95,
  continuous_support = c("real_line", "positive_real", "bounded_continuous",
    "bounded_score"),
  score_min = NULL,
  score_max = NULL,
  score_step = 1,
  heaping_grids = 1,
  heaping = c("shared", "arm_specific"),
  bounded_kernel = c("beta", "logit_normal"),
  mixture_components = 10,
  auto_select_mixture_components = TRUE,
  mixture_components_max = NULL,
  chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000,
  seed = NULL,
  refresh = 0,
  control = list(adapt_delta = 0.95, max_treedepth = 12),
  prior = NULL,
  ...
)

\method{trunc_comp_bayes}{default}(
  formula,
  a,
  r,
  atom = NULL,
  conf.level = 0.95,
  continuous_support = c("real_line", "positive_real", "bounded_continuous",
    "bounded_score"),
  score_min = NULL,
  score_max = NULL,
  score_step = 1,
  heaping_grids = 1,
  heaping = c("shared", "arm_specific"),
  bounded_kernel = c("beta", "logit_normal"),
  mixture_components = 10,
  auto_select_mixture_components = TRUE,
  mixture_components_max = NULL,
  chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000,
  seed = NULL,
  refresh = 0,
  control = list(adapt_delta = 0.95, max_treedepth = 12),
  prior = NULL,
  ...
)
}
\arguments{
\item{formula}{For the formula interface, a formula with a continuous outcome
on the left-hand side and a single binary treatment indicator on the
right-hand side. For the default interface, the first positional argument
is the outcome vector.}

\item{...}{Additional arguments passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}. Covariate
adjustment is not supported in the Bayesian pathway.}

\item{atom}{A single numeric atom value used for the distinguished outcome.
For the default interface, \code{atom} may be omitted only when \code{y[a == 0]} has
exactly one unique finite value.}

\item{data}{A data frame containing the variables referenced by \code{formula}.}

\item{conf.level}{Credible level used for stored posterior summaries.}

\item{continuous_support}{Support of the continuous non-atom component.
\code{"real_line"} fits the existing Gaussian-mixture model on the real line,
while \code{"positive_real"} fits a Gamma-mixture model on the positive real
line and therefore requires all non-atom outcomes to be strictly
positive. \code{"bounded_continuous"} fits an experimental Beta-mixture model
by default for finely measured bounded outcomes inside
\verb{(score_min, score_max)}. \code{"bounded_score"} fits an experimental
discretized/heaped Beta-mixture model by default for integer or grid-valued
non-atom scores in \verb{[score_min, score_max]}. The bounded models can
instead use logit-normal mixture kernels via
\code{bounded_kernel = "logit_normal"}.}

\item{score_min, score_max}{Finite numeric lower and upper bounds for the two
bounded Bayesian models. Required when \code{continuous_support} is
\code{"bounded_continuous"} or \code{"bounded_score"}.}

\item{score_step}{Positive score-grid spacing for
\code{continuous_support = "bounded_score"}. The default is \code{1}.}

\item{heaping_grids}{Positive heaping grid widths for
\code{continuous_support = "bounded_score"}. Each value must be a multiple of
\code{score_step}; the default is \code{1}.}

\item{heaping}{Whether bounded-score heaping proportions are shared across
arms (\code{"shared"}) or arm-specific (\code{"arm_specific"}).}

\item{bounded_kernel}{Kernel family for bounded Bayesian models.
\code{"beta"} keeps the existing bounded Beta-mixture models and is the
default. \code{"logit_normal"} uses logit-normal mixture kernels for
\code{continuous_support = "bounded_continuous"} and
\code{continuous_support = "bounded_score"}.}

\item{mixture_components}{Initial truncation level for the arm-specific
stick-breaking mixtures. Must be at least \code{2}. The default initial level
remains \code{10}.}

\item{auto_select_mixture_components}{Logical flag indicating whether the
Bayesian fit should automatically increase the truncation level by
doubling it until sampler diagnostics pass or the maximum level is
reached.}

\item{mixture_components_max}{Optional maximum truncation level used when
\code{auto_select_mixture_components = TRUE}. If left \code{NULL}, the effective
maximum is \code{max(40, mixture_components)}.}

\item{chains}{Number of Stan chains.}

\item{iter_warmup}{Number of warmup iterations per chain.}

\item{iter_sampling}{Number of post-warmup iterations per chain.}

\item{seed}{Optional non-negative integer passed to Stan.}

\item{refresh}{Stan progress-refresh interval.}

\item{control}{Stan control list. The current implementation supports
\code{adapt_delta} and \code{max_treedepth}.}

\item{prior}{Optional named list overriding the default priors. The common
fields are \code{rho_alpha}, \code{rho_beta}, \code{alpha_shape}, and \code{alpha_rate}. For
\code{continuous_support = "real_line"}, the support-specific fields are
\code{mu_mean}, \code{mu_sd}, \code{sigma_meanlog}, and \code{sigma_sdlog}. For
\code{continuous_support = "positive_real"}, the support-specific fields are
\code{mean_meanlog}, \code{mean_sdlog}, \code{shape_meanlog}, and \code{shape_sdlog}. For
bounded Beta-mixture models, the support-specific fields are \code{m_alpha},
\code{m_beta}, \code{phi_meanlog}, and \code{phi_sdlog}. For bounded logit-normal models,
the support-specific fields are \code{mu_logit_mean}, \code{mu_logit_sd},
\code{sigma_logit_meanlog}, and \code{sigma_logit_sdlog}; bounded-score fits also
accept \code{eta_prior}, either a scalar or one value per heaping grid.}

\item{a}{Binary indicator for whether the continuous outcome is observed in
the default interface.}

\item{r}{Binary treatment indicator for the default interface.}
}
\value{
An object of class \code{"trunc_comp_bayes_fit"} containing the fitted
\code{stanfit}, posterior draws, stored posterior summaries, sampler diagnostics,
truncation-selection settings and history, the standardized analysis data,
and the matched call. Failed fits return the same class with
\code{success = FALSE} and an error message.
}
\description{
Fits an explicit two-part Bayesian model for a binary treatment comparison
with a distinguished atom value representing an undefined or unobserved
outcome. The current implementation supports only the no-covariate model,
with real-line, strictly positive, bounded continuous, or bounded reported
score non-atom outcomes. The bounded models are experimental MVPs.
}
\details{
The Bayesian pathway is experimental. It fits a two-part model with one atom
probability per treatment arm and one truncated stick-breaking mixture per
treatment arm for the non-atom outcomes. The continuous component can be
modeled with Gaussian kernels on the real line, Gamma kernels on the
positive real line, or experimental bounded Beta-mixture or logit-normal
kernels.
Likelihood-ratio statistics are not reported, but
discrepancy-based posterior predictive p-values are available through
\code{\link[=posterior_predictive_pvalues]{posterior_predictive_pvalues()}} and \code{\link[=posterior_predictive_check]{posterior_predictive_check()}} for model
checking.

The \code{mixture_components} argument now controls the initial finite
stick-breaking approximation level. When
\code{auto_select_mixture_components = TRUE}, the package fits the requested
initial level and accepts the current level when the usual sampler
diagnostics pass. If sampler diagnostics fail, the truncation level is
doubled and the model is refit until an acceptable level is found or
\code{mixture_components_max} is reached. The smallest accepted level is retained
as the final fit.

In the stored posterior draws and summaries:
\itemize{
\item \code{rho_0} and \code{rho_1} are the arm-specific atom probabilities.
\item \code{pi_0} and \code{pi_1} are the arm-specific probabilities of being observed
away from the atom, so \code{pi_r = 1 - rho_r}.
\item \code{mu_0_c} and \code{mu_1_c} are the arm-specific means among the non-atom
outcomes.
\item \code{delta_atom = rho_1 - rho_0} is the treatment-minus-control difference in
atom probability.
\item \code{mu_delta = mu_1^c - mu_0^c} is the treatment-minus-control difference in
mean among the non-atom outcomes.
\item \verb{alpha_delta = [pi_1 / (1 - pi_1)] / [pi_0 / (1 - pi_0)]} is the
treatment-to-control odds ratio for being observed away from the atom.
}

The headline posterior summary is the combined-outcome contrast

\verb{delta = [atom * rho_1 + pi_1 * mu_1^c] - [atom * rho_0 + pi_0 * mu_0^c]},

which combines the atom probability and the non-atom mean into one
treatment-minus-control contrast on the original outcome scale.
}
\examples{
\dontrun{
library(TruncComp2)
f0 <- function(n) stats::rnorm(n, 2, 1)
f1 <- function(n) stats::rnorm(n, 2.5, 1)
d <- simulate_truncated_data(20, f0 = f0, f1 = f1, pi0 = 0.6, pi1 = 0.7)
fit <- trunc_comp_bayes(Y ~ R, atom = 0, data = d, chains = 4,
                        iter_warmup = 500, iter_sampling = 1000)
summary(fit)
}
}
