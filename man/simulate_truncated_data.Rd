% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_truncated_data}
\alias{simulate_truncated_data}
\title{Simulate two-group truncated outcome data}
\usage{
simulate_truncated_data(n, f0, f1, pi0, pi1, atom = 0)
}
\arguments{
\item{n}{Number of observations per group.}

\item{f0}{Function that generates non-atom outcomes for group \code{R = 0}. It may
return a finite numeric vector of length \code{n} when called with \code{n}, or a
single finite numeric value when called repeatedly with \code{1}. Any non-atom
values used in the returned dataset must differ from \code{atom}.}

\item{f1}{Function that generates non-atom outcomes for group \code{R = 1}. It may
return a finite numeric vector of length \code{n} when called with \code{n}, or a
single finite numeric value when called repeatedly with \code{1}. Any non-atom
values used in the returned dataset must differ from \code{atom}.}

\item{pi0}{Probability of being outside the atom in group \code{R = 0}.}

\item{pi1}{Probability of being outside the atom in group \code{R = 1}.}

\item{atom}{Numeric atom value inserted into \code{Y} when \code{A = 0}.}
}
\value{
A data frame with columns \code{R} (binary treatment indicator), \code{A}
(binary non-atom indicator), and \code{Y} (coded endpoint with atom observations
represented by \code{atom}).
}
\description{
Simulates two treatment groups with a binary non-atom indicator and a
continuous outcome conditional on being outside the atom. The substantive
atom is represented by the supplied \code{atom} value in the returned \code{Y} column,
so generated non-atom outcomes must differ from \code{atom}.
}
\examples{
f0 <- function(n) stats::rnorm(n, 3, 1)
f1 <- function(n) stats::rnorm(n, 3.5, 1)
simulate_truncated_data(n = 25, f0 = f0, f1 = f1, pi0 = 0.6, pi1 = 0.5)
}
